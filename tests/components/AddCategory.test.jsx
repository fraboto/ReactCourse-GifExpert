import { render } from "@testing-library/react"
import { AddCategory } from "../../src/components/AddCategory"

describe('Tests for the component AddCategory', () => {

    test('should ', () => {
        render(<AddCategory onAddCategory={() => { }} />)
    })
})